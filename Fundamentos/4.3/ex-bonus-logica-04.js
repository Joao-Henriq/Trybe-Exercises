let n = 5 // número de asteriscos na base;
let vazio = '';
let posicao = n;
let simbolo = '*';

for (let linha = 0 ; linha < n ; linha++){
    if(linha < 1){
        vazio = '';
        console.log('1');
    }
    
    for (let coluna = 1; coluna < n ; coluna++){
    }
    if (linha === 4 ){
        console.log('*');
        break;
    }
}