def is_odd(number):
    'Retorna true se um número é impar, senão False'
    return number % 2 != 0

def divide(a_number, b_number):
    "Retorna a divisão de a_number por other_number"
    return a_number / b_number
